import { History, Search, Trash2 } from 'lucide-react'
import { useState } from 'react'
import { createSearchParams, Link, useNavigate } from 'react-router-dom'
import { SearchHistoryItem } from '../components/history/SearchHistoryItem'
import { useSearchHistory } from '../context/SearchHistoryContext'

export function HistoryPage() {
  const { history, clearHistory } = useSearchHistory()
  const [isConfirmingClear, setIsConfirmingClear] = useState(false)
  const navigate = useNavigate()
  function searchAgain(query: string) { navigate({ pathname: '/search', search: createSearchParams({ q: query }).toString() }) }
  return <section className="mx-auto w-full max-w-5xl px-4 py-6 sm:px-6 sm:py-8 lg:px-10"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-start"><div><p className="text-sm font-semibold text-blue-600">Knowledge base</p><h1 className="mt-1 text-2xl font-semibold tracking-tight text-slate-900 sm:text-3xl">Search history</h1><p className="mt-2 text-sm leading-6 text-slate-600">Your recent searches from this session.</p></div>{history.length > 0 && <button type="button" onClick={() => setIsConfirmingClear(true)} className="inline-flex items-center justify-center gap-2 rounded-lg border border-slate-200 bg-white px-4 py-2.5 text-sm font-medium text-slate-700 shadow-sm transition-colors hover:border-rose-200 hover:bg-rose-50 hover:text-rose-800 focus:outline-none focus-visible:ring-2 focus-visible:ring-blue-600 focus-visible:ring-offset-2"><Trash2 className="size-4" aria-hidden="true" />Clear history</button>}</div>{isConfirmingClear && <div className="mt-6 rounded-xl border border-amber-200 bg-amber-50 p-5 shadow-sm" role="alertdialog" aria-modal="false" aria-labelledby="clear-history-title" aria-describedby="clear-history-description"><h2 id="clear-history-title" className="text-base font-semibold text-slate-900">Clear search history?</h2><p id="clear-history-description" className="mt-2 text-sm text-slate-700">This will remove the searches from this session.</p><div className="mt-4 flex flex-wrap gap-3"><button type="button" onClick={() => setIsConfirmingClear(false)} className="rounded-lg border border-slate-300 bg-white px-3 py-2 text-sm font-medium text-slate-700 transition-colors hover:bg-slate-50 focus:outline-none focus-visible:ring-2 focus-visible:ring-blue-600 focus-visible:ring-offset-2">Cancel</button><button type="button" onClick={() => { clearHistory(); setIsConfirmingClear(false) }} className="rounded-lg bg-rose-700 px-3 py-2 text-sm font-medium text-white transition-colors hover:bg-rose-800 focus:outline-none focus-visible:ring-2 focus-visible:ring-rose-700 focus-visible:ring-offset-2">Clear history</button></div></div>}{history.length === 0 ? <div className="mt-8 rounded-xl border border-slate-200 bg-white p-8 text-center shadow-sm shadow-slate-200/40 sm:p-10"><span className="mx-auto flex size-11 items-center justify-center rounded-xl bg-blue-50 text-blue-700 ring-1 ring-blue-100"><History className="size-5" aria-hidden="true" /></span><h2 className="mt-5 text-lg font-semibold text-slate-900">No search history yet</h2><p className="mt-2 text-sm text-slate-600">Your recent searches will appear here.</p><Link to="/search" className="mt-6 inline-flex items-center gap-2 rounded-lg bg-blue-600 px-4 py-2.5 text-sm font-medium text-white shadow-sm shadow-blue-600/20 transition-colors hover:bg-blue-700 focus:outline-none focus-visible:ring-2 focus-visible:ring-blue-600 focus-visible:ring-offset-2"><Search className="size-4" aria-hidden="true" />Start searching</Link></div> : <div className="mt-8"><h2 className="border-b border-slate-200 pb-4 text-sm font-semibold text-slate-800">Recent searches</h2><div className="mt-4 space-y-3">{history.map((item) => <SearchHistoryItem key={item.id} item={item} onSearchAgain={searchAgain} />)}</div></div>}</section>
}
