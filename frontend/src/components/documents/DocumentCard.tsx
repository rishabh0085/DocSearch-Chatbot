import { CircleCheck, CircleX, FileText, LoaderCircle, MessageSquare, MoreHorizontal, Sparkles, Trash2 } from 'lucide-react'
import { useState } from 'react'
import { Link } from 'react-router-dom'
import type { DocumentResponse, DocumentStatus } from '../../types/document'
import { formatDocumentDate, formatFileSize, formatFileType } from '../../utils/documentFormatters'
type Props = { document: DocumentResponse; isDeleting: boolean; onDelete: (id: number) => Promise<boolean> }
const styles: Record<DocumentStatus, { label: string; className: string }> = { UPLOADED: { label: 'Uploaded', className: 'bg-slate-100 text-slate-700' }, PROCESSING: { label: 'Processing', className: 'bg-amber-50 text-amber-800' }, READY: { label: 'Ready', className: 'bg-emerald-50 text-emerald-800' }, FAILED: { label: 'Failed', className: 'bg-rose-50 text-rose-800' } }
export function DocumentCard({ document, isDeleting, onDelete }: Props) {
  const [confirming, setConfirming] = useState(false); const status = document.status ? styles[document.status] : { label: 'Status unavailable', className: 'bg-slate-100 text-slate-600' }
  const pageCount = document.pageCount == null ? null : `${document.pageCount} ${document.pageCount === 1 ? 'page' : 'pages'}`
  async function confirmDelete() { if (await onDelete(document.id)) setConfirming(false) }
  return <article className="rounded-2xl border border-slate-200 bg-white p-4 shadow-sm shadow-slate-200/40 transition-all hover:border-[#edc6ae] hover:shadow-md sm:p-5"><div className="flex flex-col gap-4 sm:flex-row"><span className="flex size-11 shrink-0 items-center justify-center rounded-xl bg-orange-50 text-orange-700"><FileText className="size-5" /></span><div className="min-w-0 flex-1"><div className="flex flex-wrap items-start justify-between gap-3"><div><h3 className="break-words text-base font-semibold text-slate-800">{document.fileName}</h3><p className="mt-1 text-sm text-slate-500">{formatFileType(document.fileType, document.fileName)} <span aria-hidden="true">·</span> {formatFileSize(document.fileSize)} {pageCount && <><span aria-hidden="true">·</span> {pageCount} </>}<span aria-hidden="true">·</span> Uploaded {formatDocumentDate(document.createdAt)}</p></div><span className={`inline-flex items-center gap-1.5 rounded-full px-2.5 py-1 text-xs font-semibold ${status.className}`}>{document.status === 'PROCESSING' && <LoaderCircle className="size-3 animate-spin motion-reduce:animate-none" />}{document.status === 'READY' && <CircleCheck className="size-3" />}{document.status === 'FAILED' && <CircleX className="size-3" />}{document.status === 'UPLOADED' && <span className="size-1.5 rounded-full bg-current" />}{status.label}</span></div><div className="mt-4 flex flex-wrap gap-2"><Link to={`/documents/${document.id}/summary`} className="inline-flex items-center gap-1.5 rounded-lg border border-orange-200 bg-orange-50 px-3 py-2 text-xs font-semibold text-orange-800 transition hover:bg-orange-100 focus:outline-none focus-visible:ring-2 focus-visible:ring-orange-600"><Sparkles className="size-3.5" />Summarize</Link><Link to="/chat" className="inline-flex items-center gap-1.5 rounded-lg border border-slate-200 px-3 py-2 text-xs font-semibold text-slate-700 transition hover:bg-slate-50 focus:outline-none focus-visible:ring-2 focus-visible:ring-orange-600"><MessageSquare className="size-3.5" />Chat</Link><Link to={`/documents/${document.id}/view`} className="rounded-lg border border-slate-200 px-3 py-2 text-xs font-semibold text-slate-700 transition hover:bg-slate-50 focus:outline-none focus-visible:ring-2 focus-visible:ring-orange-600">View</Link>{!confirming && <button type="button" disabled={isDeleting} onClick={() => setConfirming(true)} className="inline-flex items-center gap-1.5 rounded-lg px-2 py-2 text-xs font-medium text-slate-500 hover:bg-rose-50 hover:text-rose-700 focus:outline-none focus-visible:ring-2 focus-visible:ring-rose-600 disabled:opacity-60" aria-label={`Delete ${document.fileName}`}><MoreHorizontal className="size-4" /><span className="sr-only">Delete</span></button>}</div></div></div>{confirming && <div className="mt-4 rounded-lg border border-rose-200 bg-rose-50 p-3" role="alertdialog" aria-modal="false" aria-label={`Delete ${document.fileName}?`}><p className="text-sm font-medium text-rose-900">Are you sure you want to delete this document?</p><div className="mt-3 flex flex-wrap justify-end gap-2"><button type="button" disabled={isDeleting} onClick={() => setConfirming(false)} className="rounded-md px-3 py-1.5 text-sm font-medium text-slate-700 hover:bg-white focus:outline-none focus-visible:ring-2 focus-visible:ring-orange-600">Cancel</button><button type="button" disabled={isDeleting} onClick={() => void confirmDelete()} className="inline-flex items-center gap-1 rounded-md bg-rose-600 px-3 py-1.5 text-sm font-medium text-white hover:bg-rose-700 focus:outline-none focus-visible:ring-2 focus-visible:ring-rose-600 disabled:opacity-60"><Trash2 className="size-3.5" />{isDeleting ? 'Deleting...' : 'Delete'}</button></div></div>}</article>
}
